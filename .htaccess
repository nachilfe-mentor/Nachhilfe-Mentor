# Generated by Website Builder
# Begin

# Skip http basic authentication
<Files "sendmail.php">
	Satisfy Any
	Order allow,deny
	Allow from all
</Files>


RewriteEngine on

# Enforce SSL
# Handle non-www URLs
RewriteCond %{HTTPS} !=on
RewriteCond %{HTTP_HOST} ^nachhilfe-mentor\.de [NC]
RewriteRule ^(.*) https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Handle www URLs
RewriteCond %{HTTPS} !=on
RewriteCond %{HTTP_HOST} ^www\.nachhilfe-mentor\.de [NC]
RewriteRule ^(.*) https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# 301 Redirect <directory>/index to <directory>/
RedirectMatch 301 (.*)/index$ $1/

# Prevent mod_dir from appending a slash to directories
DirectorySlash Off

# Handle URLs without .html extension
RewriteCond %{REQUEST_METHOD} ^(GET) [NC]
RewriteCond %{DOCUMENT_ROOT}%{REQUEST_URI}.html -f
RewriteRule ^(.*) $1.html [L]

#Begin of Blog rules
# Rewrite rule for category pages (anything with a trailing slash)
RewriteRule ^blog-1/([^/]+)/$ blog-1/category.php?category=$1 [L,QSA]
# Rewrite rule for individual post pages (anything without a trailing slash)
RewriteRule ^blog-1/([^/.]+)$ blog-1/post.php?posturl=$1 [L,QSA]
#End of Blog rules

# End
# Generated by Website Builder