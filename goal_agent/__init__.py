"""Nachhilfe Mentor SEO Goal Agent.

The package is intentionally safe-by-default. Importing it never starts the
agent, changes production files, contacts external services, or reads secrets.
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
